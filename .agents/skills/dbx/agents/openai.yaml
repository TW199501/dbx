interface:
  display_name: "Dbx"
  short_description: "Repo-specific patterns and workflows for dbx"
  default_prompt: "Use the dbx repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true